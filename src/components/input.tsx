import styled from 'react-emotion';
import { colors } from 'src/styles/colors';

const TextInputModal = styled('input')({
  outline: 'none',
  backgroundColor: colors.WHITE,
  paddingTop: 10,
  paddingRight: 25,
  paddingBottom: 10,
  paddingLeft: 25,
});

export { TextInputModal };
